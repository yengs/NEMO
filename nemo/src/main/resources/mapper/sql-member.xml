<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0/EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="nemo.mapper.MemberMapper">
	<select id="selectMemberList" resultType="nemo.dto.MemberDto">
		select member_num, member_name, member_email
		     , date_format(member_date, '%Y.%m.%d %H:%i:%s') as member_date
		  from member
	</select>
	
	<insert id="insertMember" parameterType="nemo.dto.MemberDto">
		insert into member (member_nickname, member_name, member_id, member_pw, member_date, member_phone, member_email, member_address)
		values (#{member_nickname}, #{member_name}, #{member_id}, #{member_pw}, #{member_date}, #{member_phone}, #{member_email}, #{member_address})
		<selectKey keyProperty="member_num" resultType="Integer">select last_insert_id()</selectKey>
	</insert>
	
	<update id="updateMember" parameterType="nemo.dto.MemberDto">
		update member set member_pw = #{mebmer_pw}, member_phone = #{member_phone}, member_nickname = #{member_nickname},
		where member_num = #{member_num}
	</update>
	
	<delete id="deleteMember" parameterType="int">
		delete from member where member_num = #{member_num}
	</delete>
	
	<select id="login" parameterType="nemo.dto.MemberDto" resultType="nemo.dto.MemberDto">
		select * from member where member_num = #{member_num} and member_pw = #{member_pw}
	</select>
	
	<insert id="join" parameterType="nemo.dto.MemberDto">
      insert into member(member_nickname, member_name, member_id, member_pw, member_date, member_user, member_phone, member_mailauth, member_mailkey, member_email, member_address, member_warning, member_clean)
         values(#{memberNickname}, #{memberName}, #{memberId}, #{memberPw}, now(), 1, #{memberPhone}, 0, #{memberMailkey}, #{memberEmail}, #{memberAddress}, 0, 50)
   </insert>
	
</mapper>